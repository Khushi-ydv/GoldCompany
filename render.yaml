services:
  - type: web
    name: gold-card-company
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: 22.11.0
      - key: ADMIN_KEY
        generateValue: true
